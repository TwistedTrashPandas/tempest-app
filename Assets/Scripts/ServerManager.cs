using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ServerManager : MonoBehaviour
{
    public uint serverPort = 50575;

	void Start ()
    {
        //Facepunch.Steamworks.Client.Instance.Networking.SendP2PPacket()

        //Facepunch.Steamworks.ServerInit serverInit = new Facepunch.Steamworks.ServerInit(string.Empty, "Master of the Tempest");
        //serverInit.GamePort =
        //Facepunch.Steamworks.Server server = new Facepunch.Steamworks.Server(appId, serverInit);
    }
}
